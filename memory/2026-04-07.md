# 2026-04-07

- Raul asked for help finishing setup of the Apex e-commerce app (Glide frontend + Google Sheets backend + Node server in `apex/`).
- Current Apex status checked: backend base exists, Apps Script exists, metrics and sourcing-check endpoints exist, fee-sheet logic exists; still missing fuller setup polish like settings/todo/dashboard command flow and end-to-end setup guidance.
- Raul asked for help setting up Telegram.
- Telegram setup completed: bot token configured in `/data/.openclaw/openclaw.json`, gateway restarted, channel status is OK, and Raul's Telegram user ID `8701545923` was paired successfully.
- Raul explicitly wants better memory/continuity so prior work and project context are remembered across sessions.
- Raul wants the assistant to directly implement sensible suggestions by default without asking first.
- For project work, Raul wants regular progress updates without needing to ask.
- Configured local git identity for this workspace so future memory/project-state changes can be committed cleanly.
- Added stable project note at `notes/apex.md` to preserve Apex status across sessions.
- Continued Apex work: added Settings/Todos bootstrap in Apps Script, implemented `/settings`, `/todos`, `/bootstrap`, and `/command` flows in backend, updated README with Glide setup guidance, and added `.gitignore` to stop tracking `node_modules`.
- Raul provided Google Apps Script Web App URL for Apex. It was saved to `apex/.env`, and the integration was verified successfully with `/health`, `/sheets/ping`, `/bootstrap`, and `/settings`.
- Apex deployment prep was pushed successfully to GitHub repo `Bingobongo69/claw` on branch `main`, including Render config at `apex/render.yaml`.
- Render deployment completed successfully. Public Apex backend URL is `https://apex-app-610g.onrender.com`, and public `/health` plus `/settings` both work.

- Raul prefers that suggestions/proposals be executed directly without asking first.
- Raul wants regular proactive status updates during project work, and early notification of blockers instead of delayed escalation.
- Telegram setup completed on 2026-04-07: bot token configured, Raul's Telegram user id 8701545923 paired/approved, and Raul plans to use Telegram instead of WhatsApp.
- Apex project progress on 2026-04-07: upgraded Apps Script (`apex/appsscript/Code.gs`) now supports bootstrap, settings, fees, and todos flows.
- Apex backend deployment path chosen: GitHub repo `https://github.com/Bingobongo69/claw` used for deploy source.
- GitHub push eventually succeeded after Raul supplied a token with write access.
- Apex backend successfully deployed to Render.
- Live Apex backend URL: `https://apex-app-610g.onrender.com`.
- Verified live endpoints after deployment: `/health` OK and `/settings` OK.
- Next planned step after compaction: finish final Glide wiring/integration instructions against the live backend URL.
- Implemented two fixes on 2026-04-07 evening: `/metrics` now trennt Umsatz-Goal vs Profit-Goal (roadTo15k uses Umsatz while `profitGoal`/`profitGoalProgress` stay auf Gewinn) und liefert `totalRevenue`/`monthRevenue`; außerdem liest der Monatsübersicht→Fixkosten-Lookup die neueste Zahl bottom-up, sodass sourcing und metrics die aktuellen Fixkosten nutzen.
- Neue Produkt-Roadmap definiert: zuerst Listings-Audit-Tab in Glide (Scoring + Vorschläge), dann Preis/Titel-Vorschläge mit 1-Klick-eBay-Apply (inkl. OAuth mit Listing-Write), danach Repricing, Restock/Einkauf, Deal-Pipeline, Ops Cockpit und Alerts.
- EBay API Stammdaten inkl. Refresh Token erhalten und in `apex/.env` abgelegt (`EBAY_CLIENT_ID`, `EBAY_RU_NAME`, `EBAY_CERT_ID`, `EBAY_REFRESH_TOKEN`).
- Listings-Audit-Backend angefangen: `/audit/listings` ruft eBay (Sell Listing API) + heuristische Checks (Titel, Beschreibung, Preis, Bilder, SEO) und liefert Score/Vorschläge; OAuth-Code+Refresh-Token jetzt erzeugt, aber Sell Listing API antwortet 404 (Account anscheinend nicht für diese Ressource freigeschaltet) → Alternative via Trading API (`GetSellerList`) oder Feed-Export nötig.
