# 2026-04-08

- Added automatic weekly reporting pipeline to Apex backend: shared utility helpers, reusable sales-normalizer, new /reports/weekly endpoint returning summarized KPIs, daily breakdown, top products, low-margin alerts, and insights for Glide dashboard use.
- Fixed monthly fixed-cost ingestion (Monatsübersicht sheet parsing) so metrics and sourcing flows pull the latest Fixkosten row.
- Scheduled a weekly cron job (every Wednesday 09:00 UTC) that runs an isolated agent turn, pulls /metrics + /sales, and pushes a German 7-Tage-Report to Raul on Telegram.
- Cron job now uses `openai/gpt-5-nano` and delivers via dedicated Telegram bot `@ebayclawbot` (account `reports`).
- Audit-Funktion vollständig entfernt (Backend-Endpunkte + UI) und durch neuen „Warenbestand“-Tab mit Gesamtwert-Anzeige und sortierter Bestandsliste ersetzt.
- Added production-ready `automation/mailer.js` CLI (with nodemailer + template parsing + dry-run) plus root `npm run mailer` script so Raul can fire the cold outreach sequence without manual copy/paste; documented usage in `automation/SETUP.md`.
